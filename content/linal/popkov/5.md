---
title: "Лекции 9, 10"
index: 5
lectureYouTubeLink:
  - "https://www.youtube.com/watch?v=8lJF48ui9oc"
  - "https://www.youtube.com/watch?v=oTfu8ZI9ECw"
lectureVkLink:
  - "https://vk.com/video-211870343_456240016"
  - "https://vk.com/video-211870343_456240019"
author:
   name: "Худалла А.Б."
   isu: 501855
---

mjx-hidden
$\newcommand{\ihat}{\hat \imath}$
$\newcommand{\jhat}{\hat \jmath}$
$\newcommand{\pipe}{\,|\,}$
$\newcommand{\lra}{\Leftrightarrow}$
$\newcommand{\la}{\Leftarrow}$
$\newcommand{\ra}{\Rightarrow}$
$\newcommand{\mb}{\mathbb}$
$\newcommand{\br}[1]{\{{#1}\}}$
$\newcommand{\ang}[1]{\langle{#1}\rangle}$
$\newcommand{\ov}{\overline}$
$\newcommand{\argsum}[2]{\underset{#2}{\overset {#1}\sum}}$
$\newcommand{\arghuge}[1]{\huge{#1}\;\normalsize}$
$\newcommand{\tmatrix}[1]{\begin{pmatrix}#1\end{pmatrix}}$
$\newcommand{\bmatrix}[1]{\begin{Bmatrix}#1\end{Bmatrix}}$
$\newcommand{\matal}[1]{\begin{align}#1\end{align}}$
$\newcommand{\pmatrix}[1]{\begin{vmatrix}#1\end{vmatrix}}$
$\newcommand{\system}[1]{\large\left \{ \begin{align}#1\end{align} \right .\normalsize}$
$\newcommand{\mc}{\mathcal}$

### Кольца и поля

$\ang{\overbracket{R}^{\text{Ring}}, +, *}$ - кольцо ***если***:
1. По сложению - абелева группа
2. $\forall x,y \in R \qquad x(y + z) =  xy + xz \qquad (x+y)z = xz+yz$ - дистрибутивность

#### Следствия:
1. $\forall x \in R \qquad 0x = x0 = 0$
	   $xy = x(y + 0) = xy + x0$
	   $\cancel{xy} = x(y + 0) = \cancel{xy} + x0$
	   $0 = x0$
2. $\forall x, y \in R \qquad x(-y) = (-x)y = -xy \qquad 0 = x0 = x(y + (-y)) = xy + x(-y)$
	   $\ra 0 = xy + x(-y)$
	   $\ra x(-y) = -xy$
3. $\forall x,y,z \in R \qquad x(y-z)=xy-xz \qquad (x-y)z=xz -yz$

---

#### $R, Q$ - кольца

$R \times Q$ - $\text{носитель}$ $R \oplus Q$
$(a, b) + (c, d) = (a +_R c, b+_Qd)$ - покомпонентное сложение колец
$(a, b) (c, d) = (a *_R c, b*_Qd)$ - покомпонентное умножение колец

$Q \leq R \quad Q$ - $\text{подкольцо}$ $R: \qquad Q \subseteq R$
1. $Q^+$ - $\text{подгруппа}$ $R^+$
2. $\forall x,y \in Q \qquad xy \in Q$ - замкнутость относительно умножения
Кольцо ассоциативно относительно умножения $\ra$ ассоциативное кольцо
Кольцо коммутативно относительно умножения $\ra$  коммутативное кольцо
Есть нейтральный элемент по умножению $\ra$ называется "кольцом с единицей"

---

##### Примеры:
1. $\mb Z \leq \mb Q \leq \mb R$
2. Кольцо десятичных дробей $\mb Z [\frac 1{10}]$
	   По поводу формата записи - $R[\alpha] = \br{a_0 + a_1a^1+...+a_n\alpha^n | a_i \in R}$
3. $\mb Z_n = \ang{\br{\ov 0_n,...,\ov {n-1}_n}, +, *}$
4. $\ang{E^3, +, [\_,\_]}$ - не ассоциативно, не коммутативно, без $1$
5. Конечные квадратные матрицы $n \times n$ - ассоциативно, не коммутативно, с $1$
6. $2\mb Z \leq \mb Z$ - коммутативно, ассоциативно, без $1$
7. $\ang{\mb R^\mb R, \overbracket{+, *}^{\text{поточечные}}}\quad (f+x)(\underbrace x_{\mb R}) = f(x) + g(x)$
   $f,g \in Map(\mb R,\mb R)$
   $(fg)(x) = f(x)*g(x)$
   $Y^X = Map(X,Y)$
   $C^n(\mb R)\leq...\leq \underbracket{C^1(\mb R)}_{\text{дифференцируемые на }\mb R}\leq \underbracket{C(\mb R)}_{\text{непрерывные на }\mb R} \leq \mb R ^ \mb R$ - подкольца вещественных отображений
8. $\ang{\mb R ^ \mb N,+,*}$ 
   $Map(\mb N, \mb R)$
   $\text{сход. посл.}\leq\text{огр. посл.}\leq \mb R^\mb N$
	   $\text{огр. посл.}$: $|a_i| \leq c$
9. $\ang{P(M), \Delta, \cap}$ - ассоциативно, коммутативно, $A \cap \underbracket M_{\text{единица}} = A$
   Булево кольцо: $\forall x \in R \quad x*x = x$
10. $\ang{\mb R[x], +, *}$ - кольцо вещественных многочленов
11. $\ang{\mb R[x], +, \circ}$ - не кольцо:
    $f \circ (g + h) \neq f \circ g + f \circ h$
12. Пример: $a,b,c,d \in \mb R$
	$(a,b) + (c,d) = (a + c, b+d)$
	$(a,b)(c,d) = (ac, ad + bc)$
	$(a,b)((c,d)+(e,f)) = (a,b)(c+e,d+f) = (a(c+e),a(d+f)+b(c+e)) = (ac+ae,ad+af+bc+be) = (ac,ad+bc)+(ae,af+be)=(a,b)(c,d)+(a,b)(e,f)$
	
	$\mb R^2$
	$(1,0)$ - единица
	$\mb R \overbracket{\rightarrow}^{\text{вложение}} \mb R[\epsilon]$ - дуальные числа
	$a \mapsto (a, 0)$
	$(a,b) = (a,0) + (0,b) = a+\underbracket{(b,0)}_{b}\;\underbracket{(0,1)}_\epsilon = a+b*\epsilon$
	$\epsilon^2 = (0,1)(1,0) = (0,0) = 0$
	$\mb R[\epsilon] = \begin{Bmatrix} a+b*\epsilon | a,b \in \mb R \quad\begin{array} \quad\epsilon \neq 0 \\ \epsilon^2 = 0\end{array} \end{Bmatrix}$
13. $\mb R[\omega] = \br{a+b\omega | a,b \in \mb R, \omega \neq \pm 1, \omega^2 = 1}$ - кольцо двойных чисел
    $(a,b)+(c,d) = (a+c, b+d)$
    $(a,b)(c,d) = (ac + bd, ad+bc)$
    $\omega = (0,1) \neq \pm 1$
    $\omega^2 = (0,1)(0,1) = (1,0)$
14. $\br 0$ - $\text{нулевое кольцо}$ $1 = 0$
    $\ang{A,+}$ - абелева группа
    $\downarrow$
    $\ang{A, +, *}$

##### Пример:
$1 \in R$
$\underbrace{1+...+1}_{r \in N} = 0$ - $\text{такое наименьшее}$ $r$ $\text{характеристика}$ - $char\;R = r$
Если $char\;R$ нет, $r = 0$
>$char\; \mb Z = 0$
>$char\; \mb Z_n = n$

#### $R^*$ - мультипликативная группа кольца $=$ все обратимые элементы
$\mb Z^* = \br{\pm1}$
$\mb Z_n^* = \br{\ov a | \text{НОД}(a,n) = 1}$

---

$x \in R$ - нильпотент, если
$x^n = 0, n \in \mb N, x \neq 0$

##### Примеры:
1. $\mb Z_4$:
	$\ov 2^2 = \ov 2 * \ov 2 = \ov 0$
$a,b \neq 0 \quad ab = 0$ - делители нуля
2. $\mb Z_6$:
	$\ov 2 * \ov 3 = \ov 0$
3. $f_1, f_2 \in \mb R^\mb R$
	$f_1(x) = |x| + x$
	$f_2(x) = |x| - x$
	$f_1 * f_2 \equiv 0$
	<img src="CleanShot 2025-10-03 at 21.34.58@2x.png" width="400">

---

### Определение поля

>*Коммутативное и ассоциативное кольцо с единицей в котором каждый ненулевой элемент обратим - называется полем*

##### Примеры:
1. $\mb Q, \mb R$
2. $p$ - простое $\lra \mb Z_p$ - поле
### Лемма
В поле нет делителей нуля

$Д-во:$
$a,b \neq 0 \qquad ab = 0$
$\qquad\qquad\quad a^{-1}ab=a^{-1}0$
$\qquad\qquad\quad b = 0$ противоречие

##### Примеры:
1. $\mb R[\epsilon]\quad b\neq 0 \quad b\epsilon \neq 0 \quad (b\epsilon)^2 = 0$ - не поле
2. $\mb R[\omega] \quad (\underbracket {a, a}_{\neq 1})(a,-a) = (0,0) = 0$ - не поле
 
---

$F^* = F \setminus \br 0$
$K \subseteq F$
$K$ - $\text{подполе}$ $F \quad K \leq F$
1. $K$ - $\text{подкольцо}$ $F$
2. $1 \in K$
3. $0 \neq x \in K \ra x ^{-1} \in K$

##### Пример:
$\mb Q \leq \mb R$
$\mb Q \leq \underbracket{...}_{\text{много всего}} \leq \mb R$
$\mb Q[\sqrt 2] = \br{a + b\sqrt 2 \;\large |\; a,b \in \mb Q}$ - поле

$Д-во:$
$\frac 1 {a + b \sqrt 2} = \frac{a - b \sqrt 2}{a^2 - 2b^2} = \frac a{a^2 - 2b^2} - \frac b {a^2-2b^2}*\sqrt 2$
$a^2 + b^2 \neq 0 \qquad a^2 - 2b^2 = 0 \qquad \frac a b = \pm \sqrt 2 \notin \mb Q$

>Порядок кольца - мощность его носителя

---

$R, Q$ - кольца
$\phi : R \rightarrow Q$ - гомоморфизм колец, если:
$\forall x, y \in R \qquad \phi(x + y) = \phi(x) + \phi(y)$
$\qquad\qquad\qquad\phi(xy) = \phi(x) * \phi(y)$
##### Примеры:
1. $\phi : x \mapsto 0$
2. $R =$ кольцо сходящихся последовательностей: $lim: (a_i) \mapsto \underset{i \rightarrow \infty}{lim}\;a_i$
   $Q = \mb R \qquad \underset{i \rightarrow \infty}{lim}(a_i+b_i) = \underset{i \rightarrow \infty}{lim}\;a_i + \underset{i \rightarrow \infty}{lim}\;b_i$
$\chi: P(M) \rightarrow \br{0,1}^M$
$A \subseteq M \quad \chi(x) = \begin{Bmatrix}0, x\notin A\\1,x\in A\end{Bmatrix}$
$\chi_A * \chi_B$
$\chi_A + \chi_B$

3. $\ang{P(M), \Delta, \cap} \simeq \ang{\br{0, 1}^M, +, *}$
   $\chi(A \Delta B) = \chi_A + \chi_B$
   $\chi(A\cap B) = \chi_A * \chi_B$
   $Изоморфизм$ 🥳
   $P(M) \simeq \underbracket{\mb Z_2 \oplus \mb Z_2 \oplus ... \oplus \mb Z_2}_{|M|}$
---

#### Изоморфизм мультипликативной группы кольца

$x \in R^*$
$1 = \phi(1) = \phi(x*x^{-1}) = \phi(x)*\underbracket{\phi(x^{-1})}_{(\phi(x))^{-1}}$
$\phi(1) = 1$


#### Пример:
$\mb Q [\sqrt 3] \quad?\quad \mb Q [\sqrt 2]$
   Пусть $\exists \; \phi$ - изоморфизм
   $\phi(1) = 1$
   $\phi(3) = \phi(1+1+1)=\phi(1)+\phi(1)+\phi(1)=3$
   $\phi(\sqrt 3 * \sqrt 3) = \underbracket{\phi(\sqrt 3)}_{a + b\sqrt 2} * \phi(\sqrt 3) = \phi(3) = 3$
   $a^2 + 2\sqrt 2 ab + 2b^2 = 3$ - противоречие
   $\ra \mb Q [\sqrt 3] \cancel \simeq \mb Q [\sqrt 2]$

### Лемма
>Если характеристика поля больше 0, то она является простым числом

$Д-во:$
Пусть $r \notin \mb P (r = mn | m,n \in \mb N)$
$\underbrace{1+1+...+1}_r=0$
$(\underbrace{1+...+1}_m)(\underbrace{1+...+1}_n)=0$
В поле нет делителей нуля $\ra$ противоречие

### Лемма
$F$ - поле, $char\;F = p$
$\forall a,b \in F \qquad (a+b)^p = a^p + b ^p$
$(a+b)^p = \argsum p{i = 0} C^i_pa^ib^{p-i}$
$i \neq p \land i \neq p \ra C^i_p \;\vdots\; p$

##### По аналогии:

$\phi : R \rightarrow Q$ - гомоморфизм полей, если:
$\forall x, y \in R \qquad \phi(x + y) = \phi(x) + \phi(y)$
$\qquad\qquad\qquad\phi(xy) = \phi(x) * \phi(y)$
$\qquad\qquad\qquad\phi(1) = 1$ ***(!)***

>С изоморфизмом всё стандартно (добавляется биекция)
### Следствие
$\ov a \in \mb Z_p$
$(\underbrace{\ov 1+...+\ov 1}_a)^p = \ov a^p = \underbrace{\ov 1^p+...+\ov1^p}_a$
$\ov a^p = \ov a \qquad a^p \equiv a (mod\;p)$

### Лемма
>Любое поле характеристики $p$, содержит подполе изоморфное $\mb Z_p$

##### Пример:
$\mb F_4$ - поле из $4$-х элеметов
$\mb F_4 \cancel \simeq \mb Z_4 \qquad \mb F_4 = \br{0,1,a,b} \quad char\; \mb F_4 = 2 \qquad \mb Z_2 \leq \mb F_4$

| +   | 0   | 1   | a   | b   |
| --- | --- | --- | --- | --- |
| 0   | 0   | 1   | a   | b   |
| 1   | 1   | 0   | b   | a   |
| a   | a   | b   | 0   | 1   |
| b   | b   | a   | 1   | 0   |

| *   | 0   | 1   | a   | b   |
| --- | --- | --- | --- | --- |
| 0   | 0   | 0   | 0   | 0   |
| 1   | 0   | 1   | a   | b   |
| a   | 0   | a   | b   | 1   |
| b   | 0   | b   | 1   | a   |

---

$F^2 = F \times F$ - плоскость над полем $F$
$\mb R^2$ - прямая на плоскаости множества точек $F^2$, удовл. $ax + by = c$
$a,b,c \in F$ и хот бы один из $\begin{bmatrix}a,b\neq 0\\ a^2+b^2 \neq 0\end{bmatrix}$
$\mb Z_2 \times \mb Z_2$
![image](/_media/CleanShot%202025-10-03%20at%2022.28.18@2x.png)

---

### Будем сравнивать многочлены

$\ov1 x\quad \ov1 x^2$
$\ov 1 \in \mb Z_2$

| x   | 0   | 1   |
| --- | --- | --- |
| $x$ | 0   | 1   |

| x     | 0   | 1   |
| ----- | --- | --- |
| $x^2$ | 0   | 1   |
Как функции: $x \equiv x^2$ на $\mb Z_2$

---

$R$ - кольцо
$a= (a_0, a_1,a_2,...)$ - почти всюду (за исключением конечного количества) $a_i = 0$

$a\in R$
$b = (b_0, b_1, b_2,...)$
$c = a+b \quad c=(c_0,c_1,c_2,...) \qquad c_i = a_i + b_i$
$c = ab \qquad\qquad\qquad\qquad\qquad\quad\;\;\;c_i = \argsum i{j=0}a_jb_{i-j}$
$c_0 = a_0b_0$
$c_1 = a_0b_1+a_1b_0$
$c_2 = a_0b_2+a_1b_1+a_2b_0$

$c(a+b) = ca+cb$
$(a+bc = ac+bc$

### Теорема

Если $R$ - ассоциативное кольцо - новое кольцо тоже ассоциативно;
Если $R$ - коммутативное кольцо - новое кольцо тоже коммутативно;
Если $R$ - кольцо с единицей - новое кольцо тоже с единицей;

$Д-во:$
1. $R$ - коммутативно
	$c_i = \argsum i {j = 0} a_j\;b_{i-j} = \argsum i {j = 0} b_j\;a_{i-j}$
2. $R$ - кольцо с $1$
	$(1,0,0,0,...)$
3. $R$ - ассоциативное кольцо
	$(ab)c = d \quad d_n = \argsum {}{k+j=n}(ab_k)c_j = \argsum{}{k+j=n}\argsum{}{k=e+l}a_ib_l = \argsum{}{i+l+j=n}a_ib_lc_j$
	$a(bc) = f \quad f_n=\argsum{}{k+j=n}a_k(\argsum{}{j=i+l}b_ic_l)=\argsum{}{k+i+l=n}a_kb_ic_l$
	$d_n=f_n$

$x^0 = (1,0,0,...)$
$x^1 = (0,1,0,...)$
$...$
$x^n = (\underbrace{0,0,...,0}_{n},1,0,...)$

$R \rightarrow$ новое кольцо
$r\mapsto(r,0,0,...,0)$
$(a_0,a_1,...,a_n,0,...) = a_0(1,0,...) + a_1(0,1,...) +...+a_n(\underbrace{0,...,0}_n,1,0,...) = a_0+a_1x+a_2x^2+...+a_nx^n$
$R[x]$ - кольцо многочленов с одной переменной
$R[x,y]$ - кольцо многочленов с двумя переменными
$...$
$R[x_1,...,x_n]$ - кольцо многочленов с $n$ переменными

---
### Степень многочленов

$deg\;R[x] \rightarrow \mb N_0 \cup \br{-\infty}$ - наибольший номер ненулевого элемента
$deg\;(0,0,...,0) = -\infty$
### Лемма (о степенях)

>$\forall f,x \in R[x] \qquad deg(f+g) \leq max(deg\;f, deg\;g)$
>$\forall f,g \in R[x] \qquad deg(fg) \leq deg\;f + deg\;g$

##### Пример:
$\mb Z_6$:
$\ov2x^2 + \ov 4x \quad \ov 3x^3 + x$
$(\ov 2x^2 + \ov 4x)(\ov 3x^3 + x) = \ov 6x^5+\ov{12}x^4 + \ov 2 x^3 + \ov 4x^2 = \ov 2 x^3 + \ov 4x^2$

$F$ - поле $\quad F[x]^* = F^* =$ многочлены нулевой степени


---

$\newcommand{\ihat}{\hat \imath}$
$\newcommand{\jhat}{\hat \jmath}$
$\newcommand{\pipe}{\,|\,}$
$\newcommand{\lra}{\Leftrightarrow}$
$\newcommand{\la}{\Leftarrow}$
$\newcommand{\ra}{\Rightarrow}$
$\newcommand{\mb}{\mathbb}$
$\newcommand{\br}[1]{\{{#1}\}}$
$\newcommand{\ang}[1]{\langle{#1}\rangle}$$\newcommand{\ov}{\overline}$
$\newcommand{\argsum}[2]{\underset{#2}{\overset {#1}\sum}}$