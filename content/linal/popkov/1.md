---
title: "Лекции 1, 2"
index: 1
lectureYouTubeLink:
  - "https://www.youtube.com/watch?v=4TmipL_EYYk"
  - "https://www.youtube.com/watch?v=gV64YglQonE"
lectureVkLink:
  - ""
---

mjx-hidden
$\newcommand{\ihat}{\hat \imath}$
$\newcommand{\jhat}{\hat \jmath}$
$\newcommand{\pipe}{\,|\,}$
$\newcommand{\lra}{\Leftrightarrow}$
$\newcommand{\la}{\Leftarrow}$
$\newcommand{\ra}{\Rightarrow}$
$\newcommand{\mb}{\mathbb}$
$\newcommand{\br}[1]{\{{#1}\}}$
$\newcommand{\ang}[1]{\langle{#1}\rangle}$
$\newcommand{\ov}{\overline}$
$\newcommand{\argsum}[2]{\underset{#2}{\overset {#1}\sum}}$
$\newcommand{\arghuge}[1]{\huge{#1}\;\normalsize}$
$\newcommand{\tmatrix}[1]{\begin{pmatrix}#1\end{pmatrix}}$
$\newcommand{\bmatrix}[1]{\begin{Bmatrix}#1\end{Bmatrix}}$
$\newcommand{\matal}[1]{\begin{align}#1\end{align}}$
$\newcommand{\pmatrix}[1]{\begin{vmatrix}#1\end{vmatrix}}$
$\newcommand{\system}[1]{\large\left \{ \begin{align}#1\end{align} \right .\normalsize}$
$\newcommand{\mc}{\mathcal}$

`Попков Роман Андреевич`

---

Язык: $x, y, \cap, \cup, =, \forall, \exists$

Существует всего один обьект - $\text{множество}$
Отношение принадлежность: $\in$

---
### $ZF1$

(экзистенциональность) - множество опредляется своими элементами:
$\forall x : \forall y (x=y \lra \forall z (z \in x \lra y \in y))$

$x \subseteq y \equiv \forall z (z \in x \ra z \in y)$
Cобственное подмножество $x \subset y = (x \subseteq y \land x \neq y)$
$\exists x: \forall y : y \notin x$
## $ZF2$

(аксиома существования) - Не существует двух разных пустых множеств, т.к. по ZF1 они определяются элементами, которых в пустом множестве нет.
Значок - ∅

### ZF3

(аксиома пары) - ∀ x, y (z = {x, y}): *неупорядоченная пара*

x, y ({x,y} = {y,x})
x = y ({x,y} = {x,x} = {x}) = $\text{singleton}$

---

{∅} ≠ ∅ (since there is no elements to compare by)

*Упорядоченная пара* $(x, y) = \{\{x\}, \{x,y\}\}$
*Упорядоченная тройка* - ...
...
*Упорядоченная n-ка* - ...

---

## T
(a,b) = (c,d) <=> a = c & b = d
>Доказательство: упражнение

---

### ZF4
(аксиома обьединения)

y = ∪ x

x, y ⇢ (ZF3) {x,y} ⇢ ∪ {x, y}

>Вывод: всё что конечно, точно является множеством
### ZF5
(аксиома бесконечности)

∃ x (x = {∅, x})
{∅, {∅}, {∅,{∅}}, ...} = $N_0$

### ZF6
(схема аксиомы выделения)

∀ x, $\phi$
∃ {y ∈ x | $\phi$ (y)}
>Пример: x = $N$ {y ∈ $N$ | ∃ (y = 2z)} = 2$N$

## T (парадокс Рассела)
*"Не существует множества всех множеств"*

Пусть существует. По ZF6 выделим в нём подмножество y = {z ∈ x | z ∉ z}
Тогда: y ∈ y <=> y ∉ y

## T

$U$ - универсум (≈ класс) `???`

Определим ∩:
x ∩ y = {∃ z | z ∈ x | y ∈ y}
x \ y = {∃ z | z ∈ x | y ∉ y}
x △ y = (x ∪ y) \ (x ∩ y) = (x \ y) ∪ (y \ x)

### ZF7

(аксиома степени / boolean)

∀ x ∃ P(x) = $2^x$ всех подмножеств x (∀ y | y ⊆ x | y ⊆ P(x))
∀ x ∅ ⊆ x
> Упражнение
> x = {a,b,c}
> P(x) = {∅, {a}, {b}, {c}, {a,b}, {b,c}, {a,c}, {a,b,c}}

{a} ∈ P(X) {a} ⊆ X

x ∈ P(X) <=> x ⊆ X

### ZF8

(аксиома регулярности)

x ∩ y = ∅
{x} ∩ x = ∅

### C

(аксиома выбора)

∀ x | x not empty, consists of non-intersecting, non-empy subsets

---

### Декартово произведение множеств

X × Y = { (x,y) | x ∈ X, y ∈ y }
>Упражнение:
>Почему X × Y - множество?
>X = {1,2}, Y = {a,b,c}
>{ (1,a), (1,b), (1,c), (2,a), (2,b), (2,c), (3,a), (3,b), (3,c) }

X × X × ... × X = $X^n$ - Декартова степень
$X^0$ = {()} = {∅}

R ⊆ X × Y - отношение между X и Y
R ⊆ X × X - бинарное отношение на множестве X

Диагональ  - $△_x$ = {(x,x) | x ∈ X}

Отображения (функции)
f ⊆ X × Y - отображения из X в Y если
1) ∀ x ∈ X ∃ y ∈ Y (x,y) ∈ f
2) ($x_1$,$y_1$) ∈ f & ($x_1$, $y_1$) => $y_1 = y_2$
X - область определения (D)
Y - область значений/кообласть (R)

*Отображение определяется D(f), R(f), f*

*Отображения f и g равны если:*
1) D(f) = D(g) и R(f) = R(g)
2) ∀ x ∈ D(f), f(x) = g(x)

>Пример: $sin R → R$, x ↦ $sin(x)$
>$sin R → [-1,1]$, x ↦ $sin(x)$

Все отображения из X в Y обозначаются как:
$Y^X$, Func(X, Y), Map(X, Y)

0 = ∅
1 = {∅}
2 = {∅, {∅}}, {0, 1}

$2^N$ = {0,1}$^N$

(x, y) ∈ f, y = f(x), f: X -> Y

A ⊆ X
f(A) (образ множества A) = {y ∈ Y | y = f(x), x ∈ A}

f$^{-1}$ (B) = {x ∈ X | y = f(x), y ∈ B}

>Пример: f: R -> R
>$x$ ↦ $x^2$

f$^{-1}$(4) = {2,-2}
f$^{-1}$(4) = {∅}

---

f(X) ⊆ Y

f: X -> Y отображение
1) Иньекция (Injection) $x_1 \neq x_2 => f(x_1) \neq f(x_2)$ *у всех x разный y*
	- $x_1 → y_2$
	- $x_2 → y_3$
	- $x_3 → y_1$
	- $x_4 → -$
	Каждому образу соотвествует ровно один прообраз
2) Cюрьекция (Surjection) (отбражение на) f(X) = y *все y покрыты*
	Каждому образу соответствует не менее одного прообраза
	- $x_1 → y_2$
	- $x_2 → y_3$
	- $x_1 → y_3$
	- $x_3 → y_1$
3) Биекция (Bijection) = сюрьекция + иньекция: 1 to 1 map

---
Композиция отображений (g ◌ f)(x) = g(f(x))

### T
(ассоциативность композиций)
Если одна из композиций h ◌ (g ◌ f) и  (h ◌ g) ◌ f

f: X -> Y f(x) = y
g: Y -> Z g(y) = z
h: Z -> T
D = X
R = T
h((g ◌ f) x) = h(g(f(x)))
((h ◌ g) ◌ f)(x) = h(g(f(x)))

Замечание $f ◌ g \neq g ◌ f$
$f(g(x))\neq g(f(x))$

f, g: $R^2 → R^2$
f = отображение относительно $ox$
g = поворот на 90° оносительно 0 🔄

(f ◌ g)(F) = f(g(A)) = C
>g(A) = B


### Тождественное отображение:
X → X
x ↦ x

f: X → Y
g: Y → X

f(g(x)) "X → Y → X" - "левое обратное"
g(x(x)) "Y → X → Y" - "правое обратное"

*Если есть и левое обратное, и правое обратное, то они равны*
Доказательство:
$g_L$ - левое обратное $g_L ◌ f = id_x$
$g_R$ - правое обратное $f ◌ g_R= id_y$

$g_R = (g_L ◌ f) ◌ g_R = g_L (f ◌ g_R) = g_L$

---

f: X → Y
1. $x \neq ∅$ f обратимо слева <=> f - иньекция
2. f обратимо справа <=> f - сюрьекция
3. f обратимо <=> биекция

R ⊆ $X^2$ - отношение эквивалентности (~)
1. Рефлексивно (x,x) ∈ R ∀ x, x ∈ X
2. Cимметрично (x,y) ∈ R => (y,x) ∈ R
3. Транзитивность (x,y) ∈ R & (y,z) ∈ R => (x,z) ∈ R

---

x ∈ X
$\{y \in X\} | y ~ x$

1. Любое отношение эквивалентности разбивает множество на пересекаемые классы
2. Любое разбиение множество создаёт некоторую эквивалентность

Фактор множество - множество классов эквивалентности

>Equivalence (~) seems to have something to do with the fact that a linear function can be moved around and still be called "the same function" (given that k is equal in all the functions)

Трансверсаль - множество содержащее ровно один элемент из каждого класса эквивалентности. Для любого отношения эквивалентности.